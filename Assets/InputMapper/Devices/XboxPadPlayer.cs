using XInputDotNetPure;

namespace Assets.InputMapper.Devices
{
    /// <summary>
    ///     Defines available players on a XBOX pad.
    /// </summary>
    public enum XboxPadPlayer
    {
        One = PlayerIndex.One,
        Two = PlayerIndex.Two,
        Three = PlayerIndex.Three,
        Four = PlayerIndex.Four
    }
}